node_modules/
logs/
dist/
*.db
*.db-wal
*.db-shm
.env
.env.*
!.env.sample
cookies.json

# Claude Code session data — never commit (private prompts/transcripts)
.claude/data/
.claude/scheduled_tasks*

# Downloaded media — never commit (per-client material)
downloads/
